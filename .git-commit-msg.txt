Simplify order flow: drop unused email/Telegram bot notifications

Orders and messages are delivered directly through the customer's chosen
chat channel, so the background email and Telegram bot notifications were
redundant and only produced missing-credential warnings on every request.

Also guard buildTelegramText against a null product, which crashed the
order page on first render before the product finished loading.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
